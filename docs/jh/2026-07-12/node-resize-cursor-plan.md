# 노드 크기 조절 커서 구현 계획

1. 크기 조절 가능 조건, 호버 노출, 탭 순서, 방향별 커서 우선순위를 검증하는 회귀 테스트를 추가한다.
2. 크기 조절 핸들을 조절 가능 상태에 렌더링하되 선택 전에는 숨기고 포인터 이벤트를 차단한다.
3. 노드 호버 시 모서리 핸들만, 선택 시 전체 핸들을 활성화한다.
4. 방향별 커서 선택자의 우선순위를 전역 버튼 규칙보다 높인다.
5. 실제 브라우저에서 비선택 모서리 호버, 크기 변경, 선택 후 면 핸들을 확인한다.
6. 웹 테스트와 저장소 필수 검사를 수행한다.
