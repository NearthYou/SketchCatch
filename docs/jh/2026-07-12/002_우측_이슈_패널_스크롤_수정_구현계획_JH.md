# 우측 이슈 패널 스크롤 수정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 우측 코드 패널에서 Terraform 검증 문제를 먼저 보여주고 Architecture 설계 문제까지 하나의 세로 스크롤로 탐색할 수 있게 한다.

**Architecture:** `WorkspaceIssuesPanel`이 Terraform, Architecture 순으로 두 진단 그룹을 자연 높이로 쌓고 유일한 세로 스크롤을 소유한다. 하위 진단 패널의 고정 높이와 자체 스크롤을 제거하며, 기존 진단 상태와 코드/Issues 리사이즈 경계는 유지한다.

**Tech Stack:** Next.js 16, React 19, CSS Modules, Node.js `node:test`, TypeScript

## Global Constraints

- 현재 브랜치에서 작업한다.
- 컴포넌트 구조, 진단 상태, API 계약을 변경하지 않는다.
- 코드/Issues 리사이즈 기능과 각 진단 카드 버튼 동작을 유지한다.
- 새 런타임 의존성을 추가하지 않는다.

---

### Task 1: 통합 Issues 스크롤 회귀 수정

**Files:**
- Modify: `apps/web/features/workspace/workspace-right-panel-layout.test.ts`
- Modify: `apps/web/features/workspace/WorkspaceIssuesPanel.module.css`
- Modify: `apps/web/features/workspace/WorkspaceIssuesPanel.tsx`
- Modify: `apps/web/features/workspace/TerraformIssuesPanel.module.css`

**Interfaces:**
- Consumes: `WorkspaceIssuesPanel`의 CSS Module 클래스 `issuesPanel`, `terraformIssues`
- Produces: Terraform을 먼저 표시하고 Architecture까지 감싸는 단일 세로 스크롤 레이아웃 계약

- [ ] **Step 1: 실패하는 레이아웃 회귀 테스트 작성**

`workspace-right-panel-layout.test.ts`의 CSS 소스 상수에 다음을 추가한다.

```ts
const workspaceIssuesStylesSource = readWorkspaceFile("WorkspaceIssuesPanel.module.css");
```

다음 테스트를 추가한다.

```ts
test("combined architecture and terraform issues share one reachable vertical scroll", () => {
  const combinedIssuesRule = getCssRule(workspaceIssuesStylesSource, "issuesPanel");
  const terraformIssuesRule = getCssRule(workspaceIssuesStylesSource, "terraformIssues");
  const nestedIssuesRule = getCssRule(terraformIssuesStylesSource, "issuesPanel");
  const nestedDiagnosticsRule = getCssRule(terraformIssuesStylesSource, "terraformDiagnostics");
  const terraformPanelIndex = workspaceIssuesPanelSource.indexOf("<TerraformIssuesPanel");
  const architecturePanelIndex = workspaceIssuesPanelSource.indexOf("<ArchitectureIssuesPanel");

  assert.ok(terraformPanelIndex > -1);
  assert.ok(architecturePanelIndex > terraformPanelIndex);
  assert.match(combinedIssuesRule, /\bgrid-template-rows:\s*auto auto;/);
  assert.match(combinedIssuesRule, /\boverflow-y:\s*auto;/);
  assert.match(combinedIssuesRule, /\bscrollbar-gutter:\s*stable;/);
  assert.doesNotMatch(combinedIssuesRule, /\boverflow:\s*hidden;/);
  assert.doesNotMatch(terraformIssuesRule, /\boverflow:\s*hidden;/);
  assert.doesNotMatch(nestedIssuesRule, /\bheight:\s*100%;/);
  assert.doesNotMatch(nestedIssuesRule, /\boverflow:\s*hidden;/);
  assert.doesNotMatch(nestedDiagnosticsRule, /\boverflow-y:\s*auto;/);
  assert.doesNotMatch(nestedDiagnosticsRule, /\bscrollbar-gutter:\s*stable;/);
});
```

- [ ] **Step 2: 테스트를 실행해 RED 확인**

Run:

```bash
pnpm --filter @sketchcatch/web exec tsx --test features/workspace/workspace-right-panel-layout.test.ts
```

Expected: 새 테스트가 기존 Architecture 우선 순서 또는 하위 진단 패널의 독립 스크롤 때문에 실패한다.

- [ ] **Step 3: 렌더링 순서와 스크롤 소유권 최소 수정**

`WorkspaceIssuesPanel.module.css`을 다음과 같이 변경한다.

```css
.issuesPanel {
  align-content: start;
  display: grid;
  grid-template-rows: auto auto;
  height: 100%;
  min-height: 0;
  overflow-y: auto;
  scrollbar-gutter: stable;
}

.terraformIssues {
  min-height: 0;
}
```

`WorkspaceIssuesPanel.tsx`에서 `<TerraformIssuesPanel>`을 `<ArchitectureIssuesPanel>`보다 먼저 렌더링한다. `TerraformIssuesPanel.module.css`에서는 하위 `.issuesPanel`의 `height: 100%`, `overflow: hidden`을 제거하고 행 높이를 `auto`로 바꾸며, `.terraformDiagnostics`의 `overflow-y`와 `scrollbar-gutter`를 제거한다.

- [ ] **Step 4: 회귀 테스트를 실행해 GREEN 확인**

Run:

```bash
pnpm --filter @sketchcatch/web exec tsx --test features/workspace/workspace-right-panel-layout.test.ts
```

Expected: 모든 테스트가 통과한다.

- [ ] **Step 5: 관련 및 저장소 필수 검사 실행**

Run:

```bash
pnpm --filter @sketchcatch/web exec tsx --test features/workspace/workspace-right-panel-layout.test.ts features/workspace/architecture-diagnostics-state.test.ts features/workspace/terraform-issues-state.test.ts
pnpm lint
pnpm typecheck
pnpm build
pnpm harness:check
git diff --check
```

Expected: 모든 명령이 exit code 0으로 끝난다.

- [ ] **Step 6: 하네스 기록 갱신 및 커밋**

`agent-progress.md`에 원인, 변경 파일, 검증 명령, 남은 위험을 영어로 간결하게 기록한다. `feature_list.json`은 기존 기능 상태를 바꾸지 않으며, 이번 버그 수정과 직접 연결되는 활성 workstream이 있을 때만 검증 증거를 갱신한다.

```bash
git add apps/web/features/workspace/workspace-right-panel-layout.test.ts \
  apps/web/features/workspace/WorkspaceIssuesPanel.module.css \
  agent-progress.md
git commit -m "Fix: 우측 이슈 패널 스크롤 잘림 수정"
```
