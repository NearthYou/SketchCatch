# Task 3 Terraform 출력 보고서

## 변경 파일

- `apps/api/src/services/terraform/terraform-nested-blocks.ts`
  - `aws_autoscaling_policy.targetTrackingConfiguration`을 Terraform nested block으로 등록했다.
- `apps/api/src/services/terraform/infrastructure-graph.ts`
  - ASG의 `desiredCapacity` 및 `desired_capacity`는 number 값일 때만 renderable config에 남긴다. 따라서 키 없음, `null`, 빈 문자열, 문자열 숫자는 출력하지 않고 `0`은 보존한다.
- `apps/api/src/services/terraform/diagram-to-terraform.test.ts`
  - Security Group ingress/egress, Listener default_action과 target group reference, Autoscaling Policy nested hierarchy, CloudWatch Alarm dimensions와 policy ARN action HCL 문자열을 추가했다.
- `apps/api/src/services/terraform/infrastructure-graph.test.ts`
  - ASG desired capacity의 누락, `null`, camelCase/snake_case 빈 문자열, 문자열 숫자, 숫자 0 처리를 추가했다.
- `apps/api/src/services/terraform/terraform-preview.test.ts`
  - DiagramJson에서 ASG `desiredCapacity`의 `null`/빈 문자열 omission 및 0 출력 HCL을 추가했다.

## TDD 증거

### RED

1. 다음 명령은 3건 실패했다.

```bash
pnpm --filter @sketchcatch/api exec tsx --test src/services/terraform/diagram-to-terraform.test.ts src/services/terraform/infrastructure-graph.test.ts src/services/terraform/terraform-preview.test.ts
```

- Autoscaling Policy가 `target_tracking_configuration = { ... }` object attribute로 출력되어 required nested block hierarchy와 달랐다.
- ASG `desiredCapacity: null` 및 빈 문자열이 renderable config에 남았다.
- 결과 HCL에도 `desired_capacity = null` 및 `desired_capacity = ""`가 출력됐다.

2. 문자열 숫자 입력을 추가한 뒤 다음 명령은 1건 실패했다.

```bash
pnpm --filter @sketchcatch/api exec tsx --test src/services/terraform/infrastructure-graph.test.ts
```

- ASG `desiredCapacity: "2"`가 renderable config에 남아 number-only 요구를 위반했다.

### GREEN

```bash
pnpm --filter @sketchcatch/api exec tsx --test src/services/terraform/diagram-to-terraform.test.ts src/services/terraform/infrastructure-graph.test.ts src/services/terraform/terraform-preview.test.ts
pnpm --filter @sketchcatch/api typecheck
pnpm lint
pnpm typecheck
pnpm build
pnpm harness:check
```

- focused Terraform tests: 39 passed, 0 failed.
- API typecheck, repository lint/typecheck/build, and final harness check passed.

## 자체 검토

- 변경은 지정 Terraform service/test 파일과 본 보고서에 한정했다. `apps/web/next-env.d.ts`는 수정하거나 포함하지 않았다.
- Terraform CLI, plan/apply, AWS SDK 호출 및 실제 클라우드 변경은 수행하지 않았다.
- Security Group, Listener, Autoscaling Policy nested block hierarchy와 quote 없는 Terraform reference를 HCL 문자열 assertion으로 검증했다.
- 알려진 우려사항 없음.
