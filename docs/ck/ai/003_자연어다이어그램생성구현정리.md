# 자연어 다이어그램 생성 실행 흐름 정리

이 문서는 현재 브랜치에서 구현한 Natural Language Diagramming 흐름을 코드 기준으로 따라가기 위한 정리다. 핵심은 사용자의 문장을 고정 시나리오 중 하나로 고르는 것이 아니라, 문장 안의 아키텍처 단서를 안정적으로 해석해 지원 가능한 리소스 조합으로 `ArchitectureJson` 초안을 만드는 것이다.

## 1. 목표

1. 사용자는 채팅창에 자연어 요구사항만 입력한다.
2. 요구사항이 부족하면 미리보기를 만들지 않고 쉬운 질문을 차례대로 묻는다.
3. 충분한 단서가 모이면 지원 가능한 ResourceType만 사용해 deterministic `ArchitectureJson`을 만든다.
4. 초안은 보드에 반투명 preview로만 표시되고, 사용자가 생성 버튼을 눌러야 실제 workspace board에 적용된다.
5. 같은 요구사항 단서 조합은 같은 node id, edge id, config 기준의 같은 초안을 만든다.

## 2. 전체 흐름

```text
사용자 자연어 입력
-> Workspace AI Chat Dock이 추가 질문 필요 여부 확인
-> 질문이 필요하면 질문/추천 답변을 채팅으로 표시
-> 답변이 모두 모이면 구현 리스트를 확인받음
-> POST /api/ai/architecture-draft 요청
-> backend가 요구사항 fact와 operatingProfile 계산
-> fact 조합으로 지원 리소스만 조립
-> 운영 조건 config와 guardrail metadata 적용
-> 선택적으로 LLM 설명 보강
-> frontend가 preview diagram 표시
-> 사용자 생성 승인 후 workspace board 전체 교체
```

이 흐름에서 Terraform apply, cloud mutation, Git/CI/CD handoff는 일어나지 않는다. AI 초안은 `User-Accepted Change` 경계를 넘기 전까지 실제 프로젝트 상태를 바꾸지 않는다.

## 3. 요청 계약

`CreateArchitectureDraftRequest`는 이제 `prompt`만 받는다.

```ts
type CreateArchitectureDraftRequest = {
  prompt: string;
};
```

`scenarioHint`, `budgetLevel`, `trafficLevel`, `securityPriority` 같은 별도 선택값은 초안 생성 계약에서 제거되었다. 예산, 방문자 규모, 보호 수준은 사용자가 자연어로 말한 단서에서 backend가 `operatingProfile`로 계산한다.

## 4. Frontend 책임

| 위치 | 책임 |
| --- | --- |
| [WorkspaceAiChatDock.tsx](../../../apps/web/features/workspace/WorkspaceAiChatDock.tsx) | 오른쪽 하단 채팅 dock, 채팅 기록, 자연어 입력, 질문 흐름, preview/apply/cancel/regenerate 처리 |
| [workspace-ai-clarification.ts](../../../apps/web/features/workspace/workspace-ai-clarification.ts) | 모호한 요구사항을 초보자도 답할 수 있는 질문으로 바꾸고, 답변을 자연어 prompt로 재구성 |
| [workspace-ai-draft-follow-up.ts](../../../apps/web/features/workspace/workspace-ai-draft-follow-up.ts) | guardrail warning 중 바로 preview하지 말고 되물어야 하는 경우를 처리 |
| [workspace-ai-diagram-adapter.ts](../../../apps/web/features/workspace/workspace-ai-diagram-adapter.ts) | backend `ArchitectureJson`을 board용 `DiagramJson`으로 변환 |
| [workspace-ai-panel-options.ts](../../../apps/web/features/workspace/workspace-ai-panel-options.ts) | 프롬프트 작성 가이드와 예시 문장만 제공 |

채팅 dock은 프로젝트별 `localStorage` key로 최근 대화 기록을 보존한다. 사용자가 질문에 답하는 중이면 draft API를 호출하지 않고, 모든 질문이 끝난 뒤에만 초안을 요청한다.

## 5. API 흐름

Architecture Draft API는 [apps/api/src/routes/ai.ts](../../../apps/api/src/routes/ai.ts)의 다음 endpoint다.

```text
POST /api/ai/architecture-draft
```

route는 `prompt`를 검증한 뒤 [aiArchitectureDrafts.ts](../../../apps/api/src/services/aiArchitectureDrafts.ts)의 `createArchitectureDraft`를 호출한다.

```text
createArchitectureDraft
-> normalizeArchitectureDraftRequest
-> resolveArchitectureRequirement
-> createDraftFromRequirementFacts
-> applyOperatingConditionConfig
-> applyGuardrailMetadata
-> AiArchitectureDraftResult 반환
```

`resolveArchitectureRequirement`는 고정 시나리오 점수를 고르는 단계가 아니라, 자연어에서 `requirementFacts`, `operatingProfile`, 지원 불가/대체 warning을 뽑는 단계다. `selectedDraftPattern`은 UI와 LLM 설명을 위한 대표 패턴 라벨이며, 실제 리소스 생성은 `requirementFacts`를 기준으로 한다.

## 6. 요구사항 fact 해석

[aiArchitectureRequirementResolution.ts](../../../apps/api/src/services/aiArchitectureRequirementResolution.ts)는 prompt에서 다음 종류의 단서를 찾는다.

```ts
type ArchitectureRequirementFact =
  | "web_frontend"
  | "static_delivery"
  | "server_runtime"
  | "database"
  | "object_storage"
  | "file_upload"
  | "auth_or_user_data"
  | "serverless_runtime"
  | "network_boundary"
  | "iam_permissions"
  | "observability"
  | "encryption";
```

예를 들어 `웹사이트`, `업로드`, `로그인`, `개인정보`, `저렴하게 시작` 같은 표현은 각각 frontend, object storage, user data, encryption, low budget 운영 조건으로 연결된다. `DB 없이`처럼 명시적인 제외 표현이 있으면 database 관련 fact를 제거한다.

아키텍처 단서가 전혀 없거나 지원 가능한 대체 구조를 만들 수 없으면 draft를 생성하지 않고 `400 bad_request`를 반환한다. frontend는 이 오류를 질문 흐름으로 바꿔 사용자에게 다시 묻는다.

## 7. 리소스 조립

[aiArchitectureRequirementDraftBuilder.ts](../../../apps/api/src/services/aiArchitectureRequirementDraftBuilder.ts)는 fact 조합을 보고 필요한 리소스를 조립한다.

| fact | 대표 생성 리소스 |
| --- | --- |
| `static_delivery` | `S3`, `CLOUDFRONT` |
| `server_runtime` | `VPC`, `SUBNET`, `INTERNET_GATEWAY`, `ROUTE_TABLE`, `SECURITY_GROUP`, `AMI`, `EC2` |
| `database` | DB subnet, DB security group, `RDS`, `KMS_KEY`, DB alarm |
| `object_storage` / `file_upload` | upload용 `S3` bucket, runtime permission edge |
| `serverless_runtime` | `API_GATEWAY_REST_API`, `LAMBDA`, `LAMBDA_PERMISSION`, execution role, logs, alarm |
| `iam_permissions` | `IAM_ROLE`, `IAM_POLICY`, `IAM_INSTANCE_PROFILE` |
| `observability` | `CLOUDWATCH_LOG_GROUP`, `CLOUDWATCH_METRIC_ALARM` |
| `encryption` | `KMS_KEY`, encrypted storage/log config |

지원하지 않는 리소스를 가짜 node로 만들지 않는다. 가능한 경우 지원 리소스로 대체하고 `unsupported_requirement_substituted` warning을 남기며, 불가능한 경우 지원 가능한 부분만 만들거나 질문 흐름으로 되돌린다.

## 8. 운영 조건 반영

[aiArchitectureOperatingConditions.ts](../../../apps/api/src/services/aiArchitectureOperatingConditions.ts)는 자연어에서 계산된 `operatingProfile`을 config에 반영한다.

| 자연어 단서 | 반영 예시 |
| --- | --- |
| 저렴하게, 처음엔, 연습용 | 작은 EC2/RDS class, 낮은 CloudFront price class, 짧은 log retention, S3 force destroy |
| 방문자 증가, 공개 서비스 | 더 큰 instance/storage, 모니터링 강화 |
| 로그인, 개인정보, 보호 | public access block, deletion protection, encryption, 보수적인 security group config |

즉 같은 “웹사이트 배포”라도 파일 업로드, 로그인, 비용 우선, 방문자 증가 같은 단서가 있으면 리소스와 config가 달라진다.

## 9. Deterministic 보장 방식

구조 결정은 LLM에 맡기지 않는다. backend rule이 stable keyword matching, fact 정렬, 고정 node id/edge id, 고정 layout 좌표를 사용한다.

LLM은 `llmExplanation`을 보강하는 설명 레이어일 뿐이다. LLM provider가 실패해도 `ArchitectureJson`은 rule 기반 결과로 반환된다.

## 10. Preview와 적용 경계

초안 생성이 성공하면 frontend는 `ArchitectureJson`을 `DiagramJson`으로 변환해 `context.setPreviewDiagram`에 넣는다. 이 preview는 보기 전용이며 기존 보드를 바로 바꾸지 않는다.

```text
preview 표시
-> 사용자가 생성 클릭
-> context.applyDiagramJson(...)
-> 실제 workspace board 전체 교체
```

현재 생성 모드는 전체 교체다. 기존 보드가 있으면 `board_replacement_required` warning을 카드 하단에 표시한다. 부분 patch preview/apply는 다음 단계 작업으로 남아 있다.

## 11. 테스트 포인트

| 테스트 | 확인 내용 |
| --- | --- |
| [apps/api/src/routes/ai.test.ts](../../../apps/api/src/routes/ai.test.ts) | prompt-only 계약, dynamic fact 조립, legacy field 무시, 운영 조건 config 차이, 모호한 prompt 거절 |
| [workspace-ai-clarification.test.ts](../../../apps/web/features/workspace/workspace-ai-clarification.test.ts) | 추가 질문, 추천 답변, 구현 리스트 확인, prompt-only 요청 변환 |
| [workspace-ai-draft-follow-up.test.ts](../../../apps/web/features/workspace/workspace-ai-draft-follow-up.test.ts) | guardrail warning을 preview 전 질문으로 전환하는 흐름 |
| [workspace-ai-guardrail-warning.test.ts](../../../apps/web/features/workspace/workspace-ai-guardrail-warning.test.ts) | 선택 UI 제거, warning/follow-up UI 경계 |
| [ai-workspace-api.test.ts](../../../apps/web/features/workspace/ai-workspace-api.test.ts) | frontend API client가 draft 요청에 prompt만 보내는지 확인 |

## 12. 읽는 순서

1. [packages/types/src/index.ts](../../../packages/types/src/index.ts)에서 `CreateArchitectureDraftRequest`, `ArchitectureRequirementFact`, `ArchitectureDraftOperatingProfile`, `AiArchitectureDraftResult`를 본다.
2. [apps/api/src/routes/ai.ts](../../../apps/api/src/routes/ai.ts)에서 request validation과 route 연결을 본다.
3. [aiArchitectureRequirementResolution.ts](../../../apps/api/src/services/aiArchitectureRequirementResolution.ts)에서 자연어를 fact와 operatingProfile로 바꾸는 규칙을 본다.
4. [aiArchitectureRequirementDraftBuilder.ts](../../../apps/api/src/services/aiArchitectureRequirementDraftBuilder.ts)에서 fact별 리소스 조립 방식을 본다.
5. [aiArchitectureOperatingConditions.ts](../../../apps/api/src/services/aiArchitectureOperatingConditions.ts)에서 자연어 운영 조건이 config로 들어가는 방식을 본다.
6. [WorkspaceAiChatDock.tsx](../../../apps/web/features/workspace/WorkspaceAiChatDock.tsx)에서 채팅, 질문, preview, apply 경계를 본다.

## 13. 주의사항

- Redis는 내부 Runtime Cache이며 사용자 Practice Architecture 리소스로 만들지 않는다.
- 지원하지 않는 리소스를 `UNKNOWN` node로 꾸며내지 않는다.
- 질문이 남아 있으면 preview를 표시하지 않는다.
- 현재 preview apply는 전체 교체이며, patch 방식은 별도 구현 대상이다.
- AWS-first 리소스를 사용하지만 SketchCatch 제품 설명은 provider-neutral IaC operations service 기준을 유지한다.
