# 워크스페이스 리소스 시각 동작 구현 계획

> **에이전트 작업자 필수 하위 스킬:** 이 계획을 작업별로 구현할 때 `superpowers:subagent-driven-development` 또는 `superpowers:executing-plans`를 사용한다. 각 단계는 checkbox로 추적한다.

**목표:** workspace 다이어그램의 일반 리소스를 `48 × 48` 아이콘 전용 선택 영역으로 만들고, label을 영역 밖에 배치하며, metadata 편집·신규 drop 영역 확장·dimmed 노드 상호작용을 안정화한다.

**아키텍처:** `DiagramNode.size`는 아이콘 geometry만 나타내고 label은 같은 node view의 absolute 자식으로 렌더링한다. metadata 병합은 빈 문자열과 누락 값을 구분한다. 부모 영역 확장은 신규 drop 트랜잭션에서만 고정 성장량으로 호출하며, 기존 drag와 dimmed 상태는 상호작용 가능성을 유지한다. Terraform 식별자와 data source 계약은 변경하지 않는다.

**기술 스택:** TypeScript, React 19, Next.js 16, `@xyflow/react`, Node test runner, CSS Modules, pnpm 11.8.0

## 전역 제약

- 변경 범위는 workspace 페이지가 사용하는 `apps/web/features/diagram-editor`, `apps/web/features/resource-settings`, `apps/web/features/workspace`로 제한한다.
- Region, Availability Zone, VPC, Subnet, Security Group, Auto Scaling Group의 영역 기본 크기는 유지한다.
- Terraform `resourceName`, `terraformBlockType`, 참조 문자열은 변경하지 않는다.
- 모든 일반 catalog·palette·AI fallback·Terraform sync fallback 리소스 기본 크기는 `48 × 48`이다.
- 일반 리소스 resize 최소값은 `28 × 28`로 유지한다.
- 보드 표시명만 대문자로 만들고 Terraform 식별자는 기존 소문자·고유성 규칙을 유지한다.
- icon label은 node geometry 아래 `4px` 바깥에 배치한다.
- 신규 icon drop 시에만 모든 부모 영역을 중심 기준으로 icon width·height의 두 배만큼 확장한다.
- 기존 노드 drag는 부모 영역 크기를 변경하지 않는다.
- 빈 `resourceName`과 `fileName`은 입력 중 보존하고 validation으로만 오류를 표시한다.
- dimmed 노드는 시각적으로만 흐려지며 다른 노드 클릭·선택·drag를 막지 않는다.
- 기존 `agent-progress.md` 수정과 삭제 상태인 `docs/jh`, `output/playwright` 파일은 사용자 변경이므로 덮어쓰거나 커밋하지 않는다.

## 파일 구조

- 생성 `apps/web/features/diagram-editor/resource-node-display-label.ts`: 표시명 후보 선택과 대문자 정규화.
- 생성 `apps/web/features/diagram-editor/area-node-expansion.ts`: 일반 아이콘과 부모 체인의 기하 확장.
- 수정 `DiagramNodeView.tsx`와 `diagram-editor.module.css`: DATA 배지 제거, 표시명, 아이콘 배경, resize 핸들.
- 수정 catalog·fallback·resize 관련 파일: 일반 리소스 생성 크기 절반 적용.
- 수정 `drag-transaction.ts`와 `DiagramEditor.tsx`: drag/drop 완료 뒤 공통 확장 함수 호출.
- Task 1~5는 최초 설계에 따라 완료된 중간 이력이며, Task 6~9가 최종 후속 요구사항으로 해당 크기·layout·확장 trigger를 보정한다.

---

### Task 1: 표시명과 DATA 배지 분리

**Files:**
- Create: `apps/web/features/diagram-editor/resource-node-display-label.ts`
- Create: `apps/web/features/diagram-editor/resource-node-display-label.test.ts`
- Modify: `apps/web/features/diagram-editor/DiagramNodeView.tsx`
- Modify: `apps/web/features/diagram-editor/DiagramNodeView.test.ts`
- Modify: `apps/web/features/diagram-editor/diagram-editor.module.css`

**Interfaces:**
- Consumes: `Pick<DiagramNode, "label" | "parameters" | "type">`
- Produces: `getResourceNodeDisplayLabel(node): string`

- [ ] **Step 1: 표시명 실패 테스트 작성**

```ts
const baseNode = {
  label: "AMI",
  type: "aws_ami",
  parameters: {
    fileName: "main",
    resourceName: "ami_2",
    resourceType: "aws_ami",
    terraformBlockType: "data" as const,
    values: {}
  }
} satisfies Pick<DiagramNode, "label" | "parameters" | "type">;

test("uses an explicit diagram label and uppercases it", () => {
  const node = {
    ...baseNode,
    parameters: { ...baseNode.parameters, values: { diagramLabel: "latest image" } }
  };
  assert.equal(getResourceNodeDisplayLabel(node), "LATEST IMAGE");
});

test("does not expose Terraform uniqueness suffixes", () => {
  assert.equal(getResourceNodeDisplayLabel(baseNode), "AMI");
});

test("falls back to the node type", () => {
  assert.equal(getResourceNodeDisplayLabel({ ...baseNode, label: "  " }), "AWS_AMI");
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/resource-node-display-label.test.ts`

Expected: FAIL because the module does not exist.

- [ ] **Step 3: 최소 helper 구현**

```ts
import type { DiagramNode } from "../../../../packages/types/src";

type ResourceNodeLabelInput = Pick<DiagramNode, "label" | "parameters" | "type">;

export function getResourceNodeDisplayLabel(node: ResourceNodeLabelInput): string {
  const diagramLabel = node.parameters?.values["diagramLabel"];
  const candidate =
    typeof diagramLabel === "string" && diagramLabel.trim().length > 0
      ? diagramLabel
      : node.label.trim().length > 0
        ? node.label
        : node.type;

  return candidate.trim().toLocaleUpperCase();
}
```

- [ ] **Step 4: DATA 배지 소스 실패 테스트 작성**

```ts
test("hides Terraform data source implementation labels", () => {
  assert.doesNotMatch(diagramNodeViewSource, /resourceNodeBadge/);
  assert.doesNotMatch(diagramNodeViewSource, /isDataNode/);
  assert.doesNotMatch(diagramEditorCssSource, /\.resourceNodeBadge\s*\{/);
});

test("uses the presentation-only label helper", () => {
  assert.match(diagramNodeViewSource, /getResourceNodeDisplayLabel\(node\)/);
  assert.match(
    diagramNodeViewSource,
    /isResourceNode\s*\?\s*getResourceNodeDisplayLabel\(node\)\s*:\s*getAreaNodeLabel\(node\)\.toLocaleUpperCase\(\)/s
  );
  assert.doesNotMatch(diagramNodeViewSource, /parameters\?\.resourceName\?\.trim/);
});
```

- [ ] **Step 5: RED 확인 후 컴포넌트 최소 수정**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/DiagramNodeView.test.ts`

Expected: FAIL while the badge and old label function remain.

`DiagramNodeView.tsx`에서 `isDataNode`, 배지 JSX, 로컬 `getResourceNodeLabel`을 제거한다. 일반 아이콘과 resource area는 `getResourceNodeDisplayLabel(node)`을 사용하고, design area는 `getAreaNodeLabel(node).toLocaleUpperCase()`를 사용한다. CSS의 `.resourceNodeBadge` 블록도 삭제한다.

- [ ] **Step 6: GREEN 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/resource-node-display-label.test.ts features/diagram-editor/DiagramNodeView.test.ts`

Expected: all tests PASS.

- [ ] **Step 7: 커밋**

```bash
git add apps/web/features/diagram-editor/resource-node-display-label.ts apps/web/features/diagram-editor/resource-node-display-label.test.ts apps/web/features/diagram-editor/DiagramNodeView.tsx apps/web/features/diagram-editor/DiagramNodeView.test.ts apps/web/features/diagram-editor/diagram-editor.module.css
git commit -m "Fix: 워크스페이스 리소스 표시명 정리"
```

---

### Task 2: 일반 리소스 생성 크기 절반 적용

**Files:**
- Modify: `apps/web/features/resource-settings/catalog.ts`
- Modify: `apps/web/features/resource-settings/catalog.test.ts`
- Modify: `apps/web/features/diagram-editor/constants.ts`
- Modify: `apps/web/features/diagram-editor/constants.test.ts`
- Modify: `apps/web/features/workspace/workspace-ai-diagram-adapter.ts`
- Modify: `apps/web/features/workspace/workspace-ai-diagram-adapter.test.ts`
- Modify: `apps/web/features/workspace/terraform-sync-proposals.ts`
- Modify: `apps/web/features/workspace/terraform-sync-proposals.test.ts`
- Modify: `apps/web/features/diagram-editor/node-resize-bounds.ts`
- Modify: `apps/web/features/diagram-editor/node-resize-bounds.test.ts`
- Modify: `apps/web/features/diagram-editor/resource-node-icon-size.ts`
- Modify: `apps/web/features/diagram-editor/resource-node-icon-size.test.ts`

**Interfaces:**
- Produces: catalog `62 × 48`, fallback `28 × 28`, resize min `28 × 28`.

- [ ] **Step 1: 크기 기대값 실패 테스트 작성**

```ts
// catalog.test.ts
assert.deepEqual(getResourceSize("aws_internet_gateway"), { width: 62, height: 48 });

// constants.test.ts의 기존 loop 내부
assert.deepEqual(item.nodeDefaults.size, { width: 28, height: 28 }, item.id);

// workspace-ai-diagram-adapter.test.ts
assert.deepEqual(unknownNode?.size, { width: 28, height: 28 });

// terraform-sync-proposals.test.ts
assert.deepEqual(createdNode?.size, { width: 28, height: 28 });

// node-resize-bounds.test.ts
assert.deepEqual(getNodeResizeBounds(makeResourceNode("aws_instance")), {
  minWidth: 28,
  minHeight: 28,
  maxWidth: 260,
  maxHeight: 260
});

// resource-node-icon-size.test.ts
assert.equal(getResourceNodeIconFrameSize({ width: 62, height: 48 }), 26);
assert.equal(getResourceNodeIconFrameSize({ width: 28, height: 28 }), 14);
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/resource-settings/catalog.test.ts features/diagram-editor/constants.test.ts features/workspace/workspace-ai-diagram-adapter.test.ts features/workspace/terraform-sync-proposals.test.ts features/diagram-editor/node-resize-bounds.test.ts features/diagram-editor/resource-node-icon-size.test.ts`

Expected: FAIL with current `124 × 96` and `56 × 56` values.

- [ ] **Step 3: 크기 상수 최소 수정**

```ts
// resource-settings/catalog.ts
const size = { width: 62, height: 48 };

// diagram-editor/constants.ts and fallback files
const DEFAULT_RESOURCE_SIZE = { width: 28, height: 28 } as const;
const DEFAULT_NODE_SIZE = { width: 28, height: 28 };
const DEFAULT_CREATED_NODE_SIZE = { width: 28, height: 28 } as const;

// node-resize-bounds.ts
const resourceNodeResizeBounds = {
  maxHeight: 260,
  maxWidth: 260,
  minHeight: 28,
  minWidth: 28
};
```

- [ ] **Step 4: 작은 icon frame 비율 구현**

```ts
const RESOURCE_NODE_LABEL_RESERVE_PX = 22;
const RESOURCE_NODE_ICON_MIN_SIZE_PX = 12;

export function getResourceNodeIconFrameSize(nodeSize: ResourceNodeSize): number {
  const usableWidth = Math.max(0, nodeSize.width);
  const labelReserve = Math.min(RESOURCE_NODE_LABEL_RESERVE_PX, Math.max(0, nodeSize.height / 2));
  const usableHeight = Math.max(0, nodeSize.height - labelReserve);
  const fittedSize = Math.min(usableWidth, usableHeight);
  return Math.round(Math.max(RESOURCE_NODE_ICON_MIN_SIZE_PX, fittedSize));
}
```

- [ ] **Step 5: GREEN 확인**

Task 2 Step 2의 명령을 다시 실행한다.

Expected: all tests PASS, including unchanged area-size assertions.

- [ ] **Step 6: 커밋**

```bash
git add apps/web/features/resource-settings/catalog.ts apps/web/features/resource-settings/catalog.test.ts apps/web/features/diagram-editor/constants.ts apps/web/features/diagram-editor/constants.test.ts apps/web/features/workspace/workspace-ai-diagram-adapter.ts apps/web/features/workspace/workspace-ai-diagram-adapter.test.ts apps/web/features/workspace/terraform-sync-proposals.ts apps/web/features/workspace/terraform-sync-proposals.test.ts apps/web/features/diagram-editor/node-resize-bounds.ts apps/web/features/diagram-editor/node-resize-bounds.test.ts apps/web/features/diagram-editor/resource-node-icon-size.ts apps/web/features/diagram-editor/resource-node-icon-size.test.ts
git commit -m "Fix: 워크스페이스 기본 리소스 크기 축소"
```

---

### Task 3: 아이콘 프레임과 resize 핸들 수정

**Files:**
- Modify: `apps/web/features/diagram-editor/diagram-editor.module.css`
- Modify: `apps/web/features/diagram-editor/DiagramNodeView.test.ts`

**Interfaces:**
- Produces: 흰색 icon frame, `8 × 8` 투명 정사각형 핸들, `1px` 테두리.

- [ ] **Step 1: CSS 실패 테스트 작성**

```ts
test("resource icon frames cover the dotted canvas grid", () => {
  assert.match(diagramEditorCssSource, /\.resourceNodeIconFrame\s*\{[^}]*background:\s*#ffffff;/s);
});

test("manual resize handles are small hollow squares", () => {
  assert.match(diagramEditorCssSource, /\.manualResizeHandle\s*\{[^}]*background:\s*transparent;[^}]*border:\s*1px solid #6f4cf6;[^}]*border-radius:\s*1px;[^}]*height:\s*8px;[^}]*width:\s*8px;/s);
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/DiagramNodeView.test.ts`

Expected: FAIL with transparent frame and filled round handles.

- [ ] **Step 3: CSS 최소 구현**

```css
.resourceNodeIconFrame { background: #ffffff; }

.manualResizeHandle {
  background: transparent;
  border: 1px solid #6f4cf6;
  border-radius: 1px;
  box-shadow: none;
  height: 8px;
  width: 8px;
}

.manualResizeHandleTopLeft { left: -4px; top: -4px; }
.manualResizeHandleTopRight { right: -4px; top: -4px; }
.manualResizeHandleBottomLeft { bottom: -4px; left: -4px; }
.manualResizeHandleBottomRight { bottom: -4px; right: -4px; }
```

- [ ] **Step 4: GREEN 확인 및 커밋**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/DiagramNodeView.test.ts`

Expected: all tests PASS.

Commit: `git commit -m "Style: 워크스페이스 리소스 선택 표시 정리"`

Stage first: `git add apps/web/features/diagram-editor/diagram-editor.module.css apps/web/features/diagram-editor/DiagramNodeView.test.ts`

---

### Task 4: 부모 영역 자동 확장 순수 함수

**Files:**
- Create: `apps/web/features/diagram-editor/area-node-expansion.ts`
- Create: `apps/web/features/diagram-editor/area-node-expansion.test.ts`

**Interfaces:**
- Produces: `expandParentAreaNodesForChildren(nodes, childNodeIds): DiagramNode[]`

- [ ] **Step 1: 단일·중첩·순환 실패 테스트 작성**

```ts
test("expands a parent with half-child margins", () => {
  const parent = makeArea("vpc", undefined, { x: 100, y: 100 }, { width: 80, height: 60 });
  const child = makeIcon("ami", parent.id, { x: 95, y: 95 }, { width: 20, height: 20 });
  const result = expandParentAreaNodesForChildren([parent, child], new Set([child.id]));
  assert.deepEqual(getNode(result, parent.id)?.position, { x: 85, y: 85 });
  assert.deepEqual(getNode(result, parent.id)?.size, { width: 95, height: 75 });
});

test("expands every nested parent inside out", () => {
  const region = makeArea("region", undefined, { x: 0, y: 0 }, { width: 100, height: 100 });
  const vpc = makeArea("vpc", region.id, { x: 70, y: 70 }, { width: 40, height: 40 });
  const child = makeIcon("ami", vpc.id, { x: 95, y: 95 }, { width: 20, height: 20 });
  const result = expandParentAreaNodesForChildren([region, vpc, child], new Set([child.id]));
  assert.deepEqual(getNode(result, vpc.id)?.size, { width: 55, height: 55 });
  assert.deepEqual(getNode(result, region.id)?.size, { width: 135, height: 135 });
});

test("stops safely for cyclic parents", () => {
  const a = makeArea("a", "b", { x: 0, y: 0 }, { width: 40, height: 40 });
  const b = makeArea("b", "a", { x: 0, y: 0 }, { width: 40, height: 40 });
  const child = makeIcon("ami", a.id, { x: 10, y: 10 }, { width: 20, height: 20 });
  assert.doesNotThrow(() => expandParentAreaNodesForChildren([a, b, child], new Set([child.id])));
});

function makeArea(
  id: string,
  parentAreaNodeId: string | undefined,
  position: DiagramNode["position"],
  size: DiagramNode["size"]
): DiagramNode {
  return {
    id,
    kind: "design",
    label: id,
    locked: false,
    ...(parentAreaNodeId ? { metadata: { parentAreaNodeId } } : {}),
    position,
    size,
    type: "design_group",
    zIndex: 0
  };
}

function makeIcon(
  id: string,
  parentAreaNodeId: string,
  position: DiagramNode["position"],
  size: DiagramNode["size"]
): DiagramNode {
  return {
    id,
    kind: "resource",
    label: "AMI",
    locked: false,
    metadata: { parentAreaNodeId },
    parameters: {
      fileName: "main",
      resourceName: "ami",
      resourceType: "aws_ami",
      terraformBlockType: "data",
      values: {}
    },
    position,
    size,
    type: "aws_ami",
    zIndex: 1
  };
}

function getNode(nodes: readonly DiagramNode[], id: string): DiagramNode | undefined {
  return nodes.find((node) => node.id === id);
}
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/area-node-expansion.test.ts`

Expected: FAIL because the module does not exist.

- [ ] **Step 3: 최소 구현**

```ts
import type { DiagramNode } from "../../../../packages/types/src";
import { isAreaNode } from "./area-nodes";

export function expandParentAreaNodesForChildren(
  nodes: readonly DiagramNode[],
  childNodeIds: ReadonlySet<string>
): DiagramNode[] {
  const nodeById = new Map(nodes.map((node) => [node.id, node]));

  for (const childNodeId of childNodeIds) {
    const child = nodeById.get(childNodeId);
    if (!child || isAreaNode(child)) continue;

    const margin = { x: child.size.width / 2, y: child.size.height / 2 };
    const visited = new Set<string>([child.id]);
    let subject = child;
    let parentId = child.metadata?.parentAreaNodeId;

    while (parentId && !visited.has(parentId)) {
      const parent = nodeById.get(parentId);
      if (!parent || !isAreaNode(parent)) break;
      visited.add(parentId);
      const expanded = expandAreaToContain(parent, subject, margin);
      nodeById.set(parent.id, expanded);
      subject = expanded;
      parentId = expanded.metadata?.parentAreaNodeId;
    }
  }

  return nodes.map((node) => nodeById.get(node.id) ?? node);
}

function expandAreaToContain(
  parent: DiagramNode,
  child: DiagramNode,
  margin: DiagramNode["position"]
): DiagramNode {
  const left = Math.min(parent.position.x, child.position.x - margin.x);
  const top = Math.min(parent.position.y, child.position.y - margin.y);
  const right = Math.max(
    parent.position.x + parent.size.width,
    child.position.x + child.size.width + margin.x
  );
  const bottom = Math.max(
    parent.position.y + parent.size.height,
    child.position.y + child.size.height + margin.y
  );

  if (
    left === parent.position.x &&
    top === parent.position.y &&
    right === parent.position.x + parent.size.width &&
    bottom === parent.position.y + parent.size.height
  ) {
    return parent;
  }

  return {
    ...parent,
    position: { x: left, y: top },
    size: { width: right - left, height: bottom - top }
  };
}
```

- [ ] **Step 4: GREEN 확인 및 커밋**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/area-node-expansion.test.ts`

Expected: all tests PASS.

Commit: `git commit -m "Feat: 워크스페이스 부모 영역 자동 확장"`

Stage first: `git add apps/web/features/diagram-editor/area-node-expansion.ts apps/web/features/diagram-editor/area-node-expansion.test.ts`

---

### Task 5: drop 및 drag 트랜잭션 연결

**Files:**
- Modify: `apps/web/features/diagram-editor/DiagramEditor.tsx`
- Modify: `apps/web/features/diagram-editor/drag-transaction.ts`
- Modify: `apps/web/features/diagram-editor/drag-transaction.test.ts`
- Modify: `apps/web/features/diagram-editor/diagram-editor-layout.test.ts`

**Interfaces:**
- Consumes: `expandParentAreaNodesForChildren`
- Produces: 신규 drop과 기존 icon drag 완료 시 동일한 확장 결과.

- [ ] **Step 1: 통합 실패 테스트 작성**

`drag-transaction.test.ts`에 다음 테스트를 추가한다.

```ts
test("finalizeDraggedNodes expands assigned parent areas for moved icon resources", () => {
  const nodes = [
    makeResourceNode({
      id: "vpc-1",
      resourceName: "main",
      resourceType: "aws_vpc",
      width: 80,
      height: 60,
      x: 0,
      y: 0
    }),
    makeResourceNode({
      id: "instance-1",
      resourceName: "web",
      resourceType: "aws_instance",
      width: 20,
      height: 20,
      x: 120,
      y: 20
    })
  ];

  const result = finalizeDraggedNodes({
    anchorNodeId: "instance-1",
    catalog: terraformParameterCatalog,
    currentNodes: nodes,
    directlyMovedNodeIds: new Set(["instance-1"]),
    positionByNodeId: new Map([["instance-1", { x: 65, y: 20 }]]),
    snapGridSize: 1,
    snapshotNodes: nodes
  });

  const vpc = result.nodes.find((node) => node.id === "vpc-1");
  const instance = result.nodes.find((node) => node.id === "instance-1");
  assert.equal(instance?.metadata?.parentAreaNodeId, "vpc-1");
  assert.deepEqual(vpc?.size, { width: 95, height: 60 });
});
```

`diagram-editor-layout.test.ts`는 신규 drop 경로가 다음 호출을 포함하는지 검증한다.

```ts
assert.match(
  diagramEditorSource,
  /expandParentAreaNodesForChildren\(nodesWithAssignedParents,\s*new Set\(\[nextNode\.id\]\)\)/s
);
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/drag-transaction.test.ts features/diagram-editor/diagram-editor-layout.test.ts`

Expected: FAIL because only parent assignment exists.

- [ ] **Step 3: drag 트랜잭션 최소 구현**

```ts
const nodesWithAssignedParents = applyAreaNodeParentAssignments(nodesWithMovedAreaChildren, movedNodeIds);
const nodesWithExpandedParents = expandParentAreaNodesForChildren(nodesWithAssignedParents, movedNodeIds);
const allMovedNodeIds = getMovedNodeIdsFromNodes(snapshotNodes, nodesWithExpandedParents);
```

반환되는 `nodes`와 reference drop target 입력도 `nodesWithExpandedParents`를 사용한다.

- [ ] **Step 4: 신규 drop 최소 구현**

```ts
const nodesWithExpandedParents = expandParentAreaNodesForChildren(
  nodesWithAssignedParents,
  new Set([nextNode.id])
);
```

`applyContainingReferenceDropTargets`에는 `nodesWithExpandedParents`를 전달한다.

- [ ] **Step 5: GREEN 확인 및 커밋**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/area-node-expansion.test.ts features/diagram-editor/drag-transaction.test.ts features/diagram-editor/diagram-editor-layout.test.ts`

Expected: all tests PASS.

Commit: `git commit -m "Feat: 워크스페이스 중첩 영역 확장 연결"`

Stage first: `git add apps/web/features/diagram-editor/DiagramEditor.tsx apps/web/features/diagram-editor/drag-transaction.ts apps/web/features/diagram-editor/drag-transaction.test.ts apps/web/features/diagram-editor/diagram-editor-layout.test.ts`

---

### Task 6: 48px 아이콘 geometry와 외부 label

**Files:**
- Modify: `apps/web/features/resource-settings/catalog.ts`
- Modify: `apps/web/features/resource-settings/catalog.test.ts`
- Modify: `apps/web/features/diagram-editor/constants.ts`
- Modify: `apps/web/features/diagram-editor/constants.test.ts`
- Modify: `apps/web/features/workspace/workspace-ai-diagram-adapter.ts`
- Modify: `apps/web/features/workspace/workspace-ai-diagram-adapter.test.ts`
- Modify: `apps/web/features/workspace/terraform-sync-proposals.ts`
- Modify: `apps/web/features/workspace/terraform-sync-proposals.test.ts`
- Modify: `apps/web/features/diagram-editor/resource-node-icon-size.ts`
- Modify: `apps/web/features/diagram-editor/resource-node-icon-size.test.ts`
- Modify: `apps/web/features/diagram-editor/diagram-editor.module.css`
- Modify: `apps/web/features/diagram-editor/DiagramNodeView.test.ts`

**Interfaces:**
- Produces: 모든 일반 리소스 기본 `size: { width: 48, height: 48 }`
- Produces: `getResourceNodeIconFrameSize({ width: 48, height: 48 }) === 48`
- Produces: icon label은 node geometry 아래 `4px` 외부에 absolute 배치

- [ ] **Step 1: 생성 경로와 icon frame 실패 테스트 작성**

기존 테스트의 일반 리소스 기대값을 다음처럼 보정한다. 영역 리소스 기대값은 바꾸지 않는다.

```ts
// resource-settings/catalog.test.ts
assert.deepEqual(getResourceSize("aws_internet_gateway"), { width: 48, height: 48 });

// diagram-editor/constants.test.ts의 일반 resource loop
assert.deepEqual(item.nodeDefaults.size, { width: 48, height: 48 }, item.id);

// workspace-ai-diagram-adapter.test.ts
assert.deepEqual(cloudFrontNode?.size, { width: 48, height: 48 });
assert.deepEqual(unknownNode?.size, { width: 48, height: 48 });

// terraform-sync-proposals.test.ts
assert.deepEqual(createdNode?.size, { width: 48, height: 48 });

// resource-node-icon-size.test.ts
test("getResourceNodeIconFrameSize uses the full square node when the label is external", () => {
  assert.equal(getResourceNodeIconFrameSize({ width: 48, height: 48 }), 48);
  assert.equal(getResourceNodeIconFrameSize({ width: 28, height: 28 }), 28);
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/resource-settings/catalog.test.ts features/diagram-editor/constants.test.ts features/workspace/workspace-ai-diagram-adapter.test.ts features/workspace/terraform-sync-proposals.test.ts features/diagram-editor/resource-node-icon-size.test.ts`

Expected: 현재 `62 × 48`, `28 × 28`, label-reserved frame 결과 때문에 FAIL.

- [ ] **Step 3: 기본 size와 icon frame 최소 구현**

```ts
// catalog.ts
const size = { width: 48, height: 48 };

// constants.ts
const DEFAULT_RESOURCE_SIZE = { width: 48, height: 48 } as const;

// workspace-ai-diagram-adapter.ts
const DEFAULT_NODE_SIZE: DiagramNode["size"] = { width: 48, height: 48 };

// terraform-sync-proposals.ts
const DEFAULT_CREATED_NODE_SIZE = { width: 48, height: 48 } as const;

// resource-node-icon-size.ts
const RESOURCE_NODE_ICON_MIN_SIZE_PX = 12;

export function getResourceNodeIconFrameSize(nodeSize: ResourceNodeSize): number {
  const fittedSize = Math.min(Math.max(0, nodeSize.width), Math.max(0, nodeSize.height));
  return Math.round(Math.max(RESOURCE_NODE_ICON_MIN_SIZE_PX, fittedSize));
}
```

- [ ] **Step 4: 외부 label CSS 실패 테스트 작성**

`DiagramNodeView.test.ts`에 다음 source assertions를 추가한다.

```ts
test("resource labels stay outside the icon-only resize geometry", () => {
  assert.match(
    diagramEditorCssSource,
    /\.resourceNodeIconFrame\s*\{[^}]*max-height:\s*100%;[^}]*max-width:\s*100%;/s
  );
  assert.match(
    diagramEditorCssSource,
    /\.resourceNodeLabel\s*\{[^}]*left:\s*50%;[^}]*position:\s*absolute;[^}]*top:\s*calc\(100% \+ 4px\);[^}]*transform:\s*translateX\(-50%\);/s
  );
});
```

- [ ] **Step 5: RED 확인 후 CSS 최소 구현**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/DiagramNodeView.test.ts`

Expected: 현재 label이 flex flow 안에 있고 icon frame이 `22px` label 공간을 예약해 FAIL.

```css
.resourceNodeIconFrame {
  max-height: 100%;
  max-width: 100%;
}

.resourceNodeLabel {
  left: 50%;
  pointer-events: none;
  position: absolute;
  top: calc(100% + 4px);
  transform: translateX(-50%);
}
```

기존 label의 `overflow-wrap`, `white-space: normal`, `width: max-content`는 유지한다.

- [ ] **Step 6: GREEN 확인과 커밋**

Task 6 Step 2 명령과 `DiagramNodeView.test.ts`를 함께 다시 실행한다.

Expected: 모든 대상 테스트 PASS, 기존 area size 테스트 PASS.

Stage only the Task 6 files and commit: `Fix: 워크스페이스 아이콘 선택 영역 정리`

---

### Task 7: Metadata 빈 입력 보존

**Files:**
- Modify: `apps/web/features/parameter-input/validation.ts`
- Modify: `apps/web/features/parameter-input/validation.test.ts`

**Interfaces:**
- Consumes: `mergeNodeParameters(node, catalog)`
- Produces: 저장된 `resourceName: ""`, `fileName: ""`을 그대로 반환

- [ ] **Step 1: 정확한 증상 회귀 테스트 작성**

```ts
test("mergeNodeParameters preserves blank editable metadata while the user types", () => {
  const node = makeResourceNode({
    label: "main",
    parameters: {
      terraformBlockType: "resource",
      resourceType: "aws_vpc",
      resourceName: "",
      fileName: "",
      values: {}
    }
  });

  const merged = mergeNodeParameters(node, makeCatalog());
  const validation = validateParameters(merged, [], [node], node.id, makeCatalog());

  assert.equal(merged.resourceName, "");
  assert.equal(merged.fileName, "");
  assert.equal(validation.metadataErrors.resourceName, "Resource name은 필수입니다.");
  assert.equal(validation.metadataErrors.fileName, "File name은 필수입니다.");
});
```

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/parameter-input/validation.test.ts`

Expected: `resourceName`과 `fileName`이 `main`으로 복구되어 FAIL.

- [ ] **Step 3: root cause 최소 수정**

`mergeNodeParameters`에서 빈 문자열을 fallback으로 처리하는 `||`를 누락 값만 처리하는 `??`로 바꾼다.

```ts
resourceName: node.parameters.resourceName ?? defaults.resourceName,
fileName: node.parameters.fileName ?? defaults.fileName,
```

- [ ] **Step 4: GREEN과 원래 repro 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/parameter-input/validation.test.ts`

Run the original assertion again:

```bash
npm exec --package=pnpm@11.8.0 -- pnpm --filter @sketchcatch/web exec tsx -e 'import assert from "node:assert/strict"; import { mergeNodeParameters } from "./features/parameter-input/validation.ts"; import { terraformParameterCatalog } from "./features/parameter-input/catalog.ts"; const node = { id: "vpc-1", type: "aws_vpc", kind: "resource", label: "main", locked: false, position: { x: 0, y: 0 }, size: { width: 240, height: 160 }, zIndex: 1, parameters: { terraformBlockType: "resource", resourceType: "aws_vpc", resourceName: "", fileName: "main", values: {} } }; assert.equal(mergeNodeParameters(node, terraformParameterCatalog).resourceName, "");'
```

Expected: validation tests PASS and original assertion exits 0.

- [ ] **Step 5: 커밋**

Stage the two Task 7 files and commit: `Fix: 리소스 이름 빈 입력 유지`

---

### Task 8: 신규 drop 전용 고정 영역 확장

**Files:**
- Modify: `apps/web/features/diagram-editor/area-node-expansion.ts`
- Modify: `apps/web/features/diagram-editor/area-node-expansion.test.ts`
- Modify: `apps/web/features/diagram-editor/DiagramEditor.tsx`
- Modify: `apps/web/features/diagram-editor/drag-transaction.ts`
- Modify: `apps/web/features/diagram-editor/drag-transaction.test.ts`
- Modify: `apps/web/features/diagram-editor/diagram-editor-layout.test.ts`

**Interfaces:**
- Produces: `expandParentAreaNodesForNewChild(nodes, childNodeId): DiagramNode[]`
- Trigger: `DiagramEditor.handleDrop` only
- Growth: each ancestor position `- child.size`, size `+ child.size * 2`

- [ ] **Step 1: 새 helper 의미의 실패 테스트 작성**

```ts
test("grows every parent by twice the new child size while preserving parent centers", () => {
  const region = makeArea("region", undefined, { x: 0, y: 0 }, { width: 300, height: 220 });
  const vpc = makeArea("vpc", region.id, { x: 40, y: 40 }, { width: 180, height: 120 });
  const child = makeIcon("instance", vpc.id, { x: 80, y: 70 }, { width: 48, height: 48 });

  const result = expandParentAreaNodesForNewChild([region, vpc, child], child.id);

  assert.deepEqual(getNode(result, vpc.id)?.position, { x: -8, y: -8 });
  assert.deepEqual(getNode(result, vpc.id)?.size, { width: 276, height: 216 });
  assert.deepEqual(getNode(result, region.id)?.position, { x: -48, y: -48 });
  assert.deepEqual(getNode(result, region.id)?.size, { width: 396, height: 316 });
  assert.deepEqual(getNode(result, child.id)?.position, child.position);
});
```

순환·누락 부모 안전 테스트는 새 단일 child ID interface에 맞춰 유지한다.

- [ ] **Step 2: RED 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/area-node-expansion.test.ts`

Expected: 현재 union/margin helper가 고정 `+96 × +96` 결과를 만들지 않아 FAIL.

- [ ] **Step 3: 고정 성장 helper 최소 구현**

```ts
export function expandParentAreaNodesForNewChild(
  nodes: readonly DiagramNode[],
  childNodeId: string
): DiagramNode[] {
  const nodeById = new Map(nodes.map((node) => [node.id, node]));
  const child = nodeById.get(childNodeId);

  if (!child || isAreaNode(child)) return [...nodes];

  const visited = new Set<string>([child.id]);
  let parentId = child.metadata?.parentAreaNodeId;

  while (parentId && !visited.has(parentId)) {
    const parent = nodeById.get(parentId);
    if (!parent || !isAreaNode(parent)) break;
    visited.add(parentId);
    const expanded = {
      ...parent,
      position: {
        x: parent.position.x - child.size.width,
        y: parent.position.y - child.size.height
      },
      size: {
        width: parent.size.width + child.size.width * 2,
        height: parent.size.height + child.size.height * 2
      }
    };
    nodeById.set(parent.id, expanded);
    parentId = expanded.metadata?.parentAreaNodeId;
  }

  return nodes.map((node) => nodeById.get(node.id) ?? node);
}
```

- [ ] **Step 4: trigger 실패 테스트 작성**

`diagram-editor-layout.test.ts`는 신규 drop에서만 새 helper를 호출하는 전체 wiring을 검증하고 `drag-transaction.ts`에는 helper 이름이 없음을 검증한다.

```ts
assert.match(
  diagramEditorSource,
  /expandParentAreaNodesForNewChild\(nodesWithAssignedParents,\s*nextNode\.id\)/s
);
assert.doesNotMatch(dragTransactionSource, /expandParentAreaNodesForNewChild/);
```

`drag-transaction.test.ts`의 기존 확장 테스트는 다음 요구로 교체한다.

```ts
test("finalizeDraggedNodes never expands a parent when an existing icon moves inside it", () => {
  const nodes = [
    makeResourceNode({
      id: "vpc-1",
      resourceName: "main",
      resourceType: "aws_vpc",
      width: 80,
      height: 60,
      x: 0,
      y: 0
    }),
    makeResourceNode({
      id: "instance-1",
      resourceName: "web",
      resourceType: "aws_instance",
      width: 20,
      height: 20,
      x: 120,
      y: 20
    })
  ];

  const result = finalizeDraggedNodes({
    anchorNodeId: "instance-1",
    catalog: terraformParameterCatalog,
    currentNodes: nodes,
    directlyMovedNodeIds: new Set(["instance-1"]),
    positionByNodeId: new Map([["instance-1", { x: 5, y: 5 }]]),
    snapGridSize: 1,
    snapshotNodes: nodes
  });

  const vpcAfter = result.nodes.find((node) => node.id === "vpc-1");
  const instanceAfter = result.nodes.find((node) => node.id === "instance-1");
  assert.equal(instanceAfter?.metadata?.parentAreaNodeId, "vpc-1");
  assert.deepEqual(vpcAfter?.position, { x: 0, y: 0 });
  assert.deepEqual(vpcAfter?.size, { width: 80, height: 60 });
});
```

- [ ] **Step 5: RED 확인 후 wiring 최소 수정**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/area-node-expansion.test.ts features/diagram-editor/drag-transaction.test.ts features/diagram-editor/diagram-editor-layout.test.ts`

Expected: drag transaction이 아직 helper를 호출하고 신규 drop이 이전 interface를 사용해 FAIL.

`DiagramEditor.handleDrop`은 parent assignment 뒤 `expandParentAreaNodesForNewChild(nodesWithAssignedParents, nextNode.id)`을 호출한다. `finalizeDraggedNodes`는 expansion import/call을 제거하고 `nodesWithAssignedParents`에서 moved IDs와 reference drop targets를 계산한다.

- [ ] **Step 6: GREEN과 커밋**

Task 8 Step 5 명령을 다시 실행한다.

Expected: 모든 대상 테스트 PASS.

Stage only Task 8 files and commit: `Fix: 신규 리소스 drop에서만 영역 확장`

---

### Task 9: Dimmed 노드 상호작용 회귀 고정

**Files:**
- Modify: `apps/web/features/diagram-editor/flow-mappers.test.ts`
- Modify: `apps/web/features/diagram-editor/diagram-editor-layout.test.ts`
- Modify only if a failing test proves necessary: `apps/web/features/diagram-editor/flow-mappers.ts`
- Modify only if a failing test proves necessary: `apps/web/features/diagram-editor/DiagramEditor.tsx`

**Interfaces:**
- Invariant: dimmed regular node has `selectable: true`, `draggable: true`, no `pointerEvents: "none"`
- Selection: clicking a different flow node replaces `selectedNodeIds` with that node ID

- [ ] **Step 1: dimmed 일반 노드 회귀 테스트 보강**

```ts
test("toFlowNodes keeps an unselected regular node clickable while another node is selected", () => {
  const vpc = makeNode({ id: "vpc-1", resourceType: "aws_vpc" });
  const instance = makeNode({ id: "instance-1", resourceType: "aws_instance" });
  const flowNodes = toFlowNodes([vpc, instance], ["vpc-1"], null, false, handlers);
  const dimmedInstance = flowNodes.find((node) => node.id === instance.id);

  assert.equal(dimmedInstance?.data.isDimmed, true);
  assert.equal(dimmedInstance?.selectable, true);
  assert.equal(dimmedInstance?.draggable, true);
  assert.equal(dimmedInstance?.style?.pointerEvents, undefined);
});
```

- [ ] **Step 2: 선택 전환 source 계약 추가**

```ts
test("clicking any interactive flow node replaces the current single selection", () => {
  assert.match(
    diagramEditorSource,
    /const handleFlowNodeClick = useCallback\([\s\S]*setSelectedNodeIds\(\[node\.id\]\)/
  );
});
```

- [ ] **Step 3: 테스트 실행과 mutation 확인**

Run: `pnpm --filter @sketchcatch/web exec tsx --test features/diagram-editor/flow-mappers.test.ts features/diagram-editor/diagram-editor-layout.test.ts`

Expected: 현재 코드가 이미 계약을 만족하면 PASS한다. 그 경우 production은 변경하지 않는다. 테스트의 red-capability는 임시로 dimmed regular node에 `pointerEvents: "none"` 또는 `selectable: false`를 적용했을 때 실패하는지 확인한 뒤 원복한다.

- [ ] **Step 4: 필요한 경우에만 최소 수정 후 GREEN**

실패가 실제 production 위반을 드러낼 때만 dimmed 조건과 pointer style을 수정한다. opacity 기반 `.nodeShellDimmed` 시각 효과는 유지한다.

- [ ] **Step 5: 커밋**

Stage only Task 9 test files와 필요한 최소 production 파일을 커밋한다: `Test: 흐려진 노드 선택 상호작용 보장`

---

### Task 10: 전체 회귀 검증과 시각 확인

**Files:**
- Modify only with preserved content: `agent-progress.md`
- Read: `clean-state-checklist.md`
- Read: `evaluator-rubric.md`

- [ ] **Step 1: web 전체 테스트**

Run: `pnpm --filter @sketchcatch/web test`

Expected: 0 failures.

- [ ] **Step 2: 필수 저장소 검사**

Run each command: `pnpm harness:check`, `pnpm lint`, `pnpm typecheck`, `pnpm build`

Expected: all commands exit 0.

- [ ] **Step 3: workspace 브라우저 확인**

- AMI와 다른 data source 아래에 `DATA`가 없다.
- 새 일반 아이콘은 `48 × 48` 정사각형이다.
- 흰색 icon frame 안에 점선 grid가 비치지 않는다.
- 선택 resize 핸들은 작고 속이 빈 정사각형이다.
- 선택 outline·연결점·resize 핸들은 아이콘만 감싸고 label은 아이콘 아래에 가려지지 않는다.
- 새 아이콘을 단일·중첩 영역에 drop하면 각 부모가 중심을 유지하며 `96 × 96` 커진다.
- 기존 아이콘을 영역 내부에서 움직여도 부모 영역 크기는 바뀌지 않는다.
- Resource name 전체를 지운 상태에서 `main`이 재생성되지 않고 새 이름을 입력할 수 있다.
- 한 노드를 선택해 다른 노드가 흐려진 상태에서도 그 다른 노드를 클릭하면 선택이 전환된다.
- 보드 표시명은 대문자이고 `_2`, `_copy`, 내부 ID가 보이지 않는다.

`playwright` 스킬 wrapper로 최신 snapshot을 만든 뒤 ref를 사용해 상호작용한다. 시각 증거가 필요하면 `output/playwright/workspace-resource-icon-final.png`로 저장한다. 기존 삭제 상태인 다른 artifact는 복원하거나 덮어쓰지 않는다.

- [ ] **Step 4: adversarial 자체 검토**

`evaluator-rubric.md`와 승인 설계를 대조한다. Terraform 식별자 불변, 신규 drop 전용 고정 성장, 기존 drag 비확장, metadata 빈 상태, dimmed 상호작용, workspace 범위, 기존 사용자 변경 보존을 확인한다.

- [ ] **Step 5: 진행 기록과 마지막 검증**

`agent-progress.md`에 영어로 목표, 변경, 검증, 위험, 다음 행동을 기존 내용과 병합해 추가한다. 그 뒤 `pnpm harness:check`, `git diff --check`, `git status --short`를 다시 실행한다.

- [ ] **Step 6: 진행 기록 보존 확인**

현재 `agent-progress.md`에는 사용자 소유의 기존 수정이 있으므로 파일 전체를 stage하지 않는다. 새 session record는 파일에 보존하되 커밋 대상에서 제외하고, 최종 보고에 그 이유를 명시한다.

Expected: 코드·테스트 커밋은 task 범위만 포함하고, 기존 사용자 수정·삭제와 새 진행 기록은 unstaged 상태로 남는다.
